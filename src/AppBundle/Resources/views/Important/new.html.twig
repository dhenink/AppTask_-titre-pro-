{% extends 'base.html.twig' %}

{% block body %}
{% include 'AppBundle:Partials:menu.html.twig' %}


<div class="container-fluid">
  <div class="row">
    <div class="col-md-8 col-md-push-2 col-xs-12">
      <div class="title-new">
        <h1>Nouvelle importance:</h1>
      </div>
      <div class="details">
        <div class="form">
          {{ form_start(form) }}

          <div class="FormImportantNom">

            <div class="importantLabel">
              <label for="form_nom">Nom de l'importance:</label>
            </div>

            <div class="importantInput">
              {{ form_errors(form.nom) }}
              {{ form_widget(form.nom) }}
            </div>
            
          </div>

          <div class="FormImportantNiveau">

            <div class="importantLabel">
              <label for="form_nom">Niveau de l'importance:</label>
            </div>

            <div class="importantInput">
              {{ form_errors(form.niveau) }}
              {{ form_widget(form.niveau) }}
            </div>

          </div>
          <div class="createI">
            <input class="btn btn-link btn-create" type="submit" value="Créer" />
          </div>
          {{ form_end(form) }}
        </div>
      </div>

      <a class="btn btn-link btn-retour" href="{{ path('important_index') }}">Retour</a>
    </div>
  </div>
</div>
{% endblock %}
